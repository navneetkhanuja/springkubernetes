spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
            name: config-example
      reload:
        enabled: true
        mode: event
        strategy: refresh